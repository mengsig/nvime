#!/usr/bin/env sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
chmod +x "$repo_root/tests/fixtures/claude"
nvim --headless -u NONE -c "set rtp^=$repo_root" -c "lua dofile('$repo_root/tests/headless_spec.lua')" -c "qa!"
